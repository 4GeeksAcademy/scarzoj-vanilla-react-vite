import { Container, Typography } from "@mui/material"

export const Footer = () => {
    return (
        <Container sx={{ backgroundColor: "gray" }}>
            <Typography>
                Copyright
            </Typography>
        </Container>
    )
}